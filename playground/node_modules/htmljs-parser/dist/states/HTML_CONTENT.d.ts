import { type StateDefinition, type Meta } from "../internal";
export interface HTMLContentMeta extends Meta {
    indent: string;
    singleLine: boolean;
    delimiter: undefined | string;
}
export declare const HTML_CONTENT: StateDefinition<HTMLContentMeta>;
